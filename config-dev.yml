server:
  port: 3344
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/final-null/sprincloud-config.git # 填写你自己的github路径
          search-paths:
            - springcloud-config # 仓库名称
      label: master # 在那个分支上
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7001/eureka
  
